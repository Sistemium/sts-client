.container.main

  ul.list-group()
    li.list-group-item(
    ng-repeat='session in vm.sessions | orderBy: "-ts" track by session.id',
    ng-class='{ disabled: session.destroyed }'
    )
      pre {{ session | json }}
